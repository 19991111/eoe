# 2026-03-13 代码优化与整合

## 1. Bug修复: 神经网络前向传播

### 问题
- `torch.matmul` 不支持批量掩码矩阵乘法
- `brain_masks` 从未真正应用到权重

### 修复
- 改用 `torch.bmm` 替代 `torch.matmul`
- 权重×掩码后再计算: `W1_masked = W1 * M1`

### 位置
- `batched_agents.py`
- `test_neural_evolution_v13.py` (HeterogeneousBrain)

---

## 2. 机制配置统一到 manifest.py

### 背景
- 原有两套配置系统: manifest.py + config/
- 需要统一管理

### 方案
```
mechanisms.yaml → PhysicsManifest.from_yaml() → IntegratedSimulation
```

### 实现
- PhysicsManifest 新增 28 个机制开关字段
- 新增 `from_yaml(preset)` 方法
- IntegratedSimulation 使用 `self.manifest` 配置
- 保留 6 个预设: full/simple/no_signal/infinite_energy/no_evolution/wrap_world

### 测试
- no_signal: ISF 场正确禁用 ✅
- no_evolution: 进化正确禁用 ✅

---

## 3. 旧版本清理清单

### 归档目录 (待清理)
- `core/eoe/_archive/environment_v11.py` (142KB)
- `config/agent_mechanisms.py` (已整合到 manifest.py)

### 未使用代码
- `fields/` (energy/impedance/stigmergy) - 未被新代码使用
- `batch/` (state/thermo/simulation) - 未被使用

### 旧版本注释
- ~50+ 处 v0.70-v12.6 注释散落各处

### 建议方案
- 方案 A: 仅删明确废弃
- 方案 B: 删除未使用代码
- 方案 C: 彻底重构

---

## 4. Git 提交

- `commit d33ad84`: fix: 修复神经网络前向传播bug
- `commit d052cbb`: feat: 统一机制配置到 manifest.py