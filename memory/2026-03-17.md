# 2026-03-17 工作日志

## 上午工作

### 1. 配置拆分完成 ✅
- 创建 `configs/` 模块，分层管理 PoolConfig
- 目录结构:
  - `configs/base.py` - 基础配置 (PoolConfig, AgentState)
  - `configs/presets/stable.py` - 稳定版配置
  - `configs/presets/experiment.py` - 实验版配置
  - `configs/utils.py` - 验证工具

- 预设配置:
  - StableConfig - 禁用非线性代谢，稳定运行
  - MinimalConfig - 调试基准测试
  - HighPressureConfig - 高压强制演化
  - V17Phase2Config - 生态稳态实验
  - DeceptiveLandscapeConfig - 课程学习
  - DifferentiableConfig - 可微演化

- 工具函数:
  - validate_config(config) - 配置验证
  - config_to_dict(config) - 转字典
  - print_config(config, category) - 按类别打印

- 向后兼容: 旧导入路径仍可用

### 2. 繁殖节点添加修复 (未完成)
- 问题: 繁殖时只修改 node_counts 计数器，没有真正添加节点
- 根因: _process_reproduction() 没有调用 mutate_add_node()
- 状态: 已定位代码位置，待修复

### 3. 能量系统调试
- 问题: Agent 能量持续下降，无法繁殖
- 根因:
  1. 能量源每10步才注入一次，需预热
  2. NONLINEAR_METABOLISM=True 会导致额外消耗
- 解决方案: 使用 StableConfig (NONLINEAR_METABOLISM=False)

## 待办
- [ ] 修复繁殖节点添加
- [ ] 运行 StableConfig 实验验证能量系统